# Real Content + Artwork Motion V1 QA

Expected captures, when browser tooling is available:

- `desktop-1440.png`
- `desktop-1280.png`
- `mobile-390.png`

The in-app browser was unavailable during this QA pass, so visual screenshot
capture and interaction inspection could not run. Manual user QA is not a
required replacement. Typecheck, lint, content audit, HTTP runtime checks and
diff validation were completed separately.
